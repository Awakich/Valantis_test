import FilterList from '@/widget/FilterList'
import { FC } from 'react'

const Filter: FC = () => {
    return (
        <section>
            <FilterList />
        </section>
    )
}

export default Filter